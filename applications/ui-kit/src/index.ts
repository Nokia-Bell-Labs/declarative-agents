// Public entry of @declarative-agents/ui-kit (srd004). KIT_VERSION tracks the
// package version; the contract major version is exported separately once the
// client lands.
export const KIT_VERSION = "0.1.0";
