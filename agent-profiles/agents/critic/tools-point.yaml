# Tool selection for the critic point machine (point.yaml).
# Loaded by run_point to build the nested point registry.

tools:
  - create_point_dir
  - copy_dir
  - copy_sample_docs
  - git_init
  - stage_all
  - commit_workspace_baseline
  - dump_config
  - run_agent
  - run_oracle_check
  - collect_trace_tokens
  - check_agent_version
  - summarize_point_results
  - collect_metrics
