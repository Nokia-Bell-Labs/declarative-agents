# A well-formed add-a-source patch: three RAG units, all fields valid.
ragUnits:
  - {name: rag0, collection: corpus, embeddingModel: qwen3-embedding:8b, replicas: 1}
  - {name: rag1, collection: corpus2, embeddingModel: qwen3-embedding:8b, replicas: 1}
  - {name: rag2, collection: corpus3, embeddingModel: qwen3-embedding:8b, replicas: 1}
executor:
  params: {nResults: 8, chunkCap: 0, routerDefault: invoke_llm_fast}
