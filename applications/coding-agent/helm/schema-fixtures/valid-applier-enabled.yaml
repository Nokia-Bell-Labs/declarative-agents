# The applier enabled with its network policy on: a values-plane deployment of the
# srd006 actuator that must lint and render.
applier:
  enabled: true
  allowApply: true
  networkPolicy:
    enabled: true
