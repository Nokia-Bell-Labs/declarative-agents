# Tool selection for the specification-critic state machine.
# Used with: agent --machine agents/specification-critic/machine.yaml --tools agents/specification-critic/tools.yaml
# Declarations live in tools/builtin.yaml.

tools:
  - load_corpus
  - validate_specs
  - jurist_rg
  - reduce_grep_checks
  - format_report
