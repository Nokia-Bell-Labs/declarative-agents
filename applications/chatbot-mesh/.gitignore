# Node dependencies for the chatbot UI SPA. Run `npm ci` in agents/chatbot/ui/app to restore.
# The built SPA (agents/chatbot/ui/app/dist) is committed, so it is intentionally not ignored.
**/node_modules/

# Persistent local integration failure artifacts.
/build/
observability/.run/
