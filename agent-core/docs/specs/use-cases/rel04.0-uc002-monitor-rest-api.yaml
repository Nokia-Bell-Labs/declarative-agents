id: rel04.0-uc002-monitor-rest-api
title: Supervisor reads monitor state through REST
status: done

summary: |
  Supervisors sometimes need a narrow read surface while an agent is still
  running. The monitor REST API provides that surface from configured
  endpoints. Agent profiles own the server address and route paths. They own
  authentication references too. Limits and OpenAPI exposure stay in the same
  trusted profile layer, which keeps HTTP callers from expanding monitor
  authority at request time.

  Handlers read cached monitor snapshots only. Responses cover machine and tool
  views plus metric summaries from the store. Recent events and streams come
  from the same cache. No handler executes tools. No handler hides a
  MachineSpec workflow step behind HTTP. Because a supervisor may reconnect
  after a long run and ask for one endpoint before opening a stream, the same
  cached snapshot must answer every view without asking a tool to refresh
  itself. Read-only behavior is the point.

actor: Supervisor reading a running agent

trigger: |
  A running agent has monitor config enabled. A supervisor needs to inspect
  current progress and metric state through the profile-owned REST API.

flow:
  - F1: The profile declares monitor REST routes and OpenAPI exposure through trusted REST config.
  - F2: Runtime starts the configured REST server using the REST server tool boundary.
  - F3: The supervisor requests the machine spec endpoint.
  - F4: The supervisor requests current machine state and selected tool metadata.
  - F5: The supervisor requests per-tool metrics and aggregate metrics.
  - F6: The supervisor requests recent events or opens the configured event stream.
  - F7: Handlers read cached monitor snapshots and apply redaction rules.
  - F8: Handler failure returns a structured REST error and does not change command or machine state.

touchpoints:
  - T1: "srd033-monitor-rest-api R1.1, R1.2, R1.3, R1.4, R1.5 -- read-only API scope and profile-owned REST authority"
  - T2: "srd033-monitor-rest-api R2.1, R2.2, R2.3, R2.4, R2.5 -- OpenAPI endpoint documentation"
  - T3: "srd033-monitor-rest-api R3.1, R3.2, R3.3, R3.4, R3.5, R3.6 -- monitor snapshot endpoints"
  - T4: "srd033-monitor-rest-api R4.1, R4.2, R4.3, R4.4, R4.5 -- event stream reads"
  - T5: "srd033-monitor-rest-api R5.1, R5.2, R5.3, R5.4, R5.5 -- redaction and failure behavior"
  - T6: "srd029-rest-server-tools R5.3, R5.5, R5.6, R5.7 -- read_state, stream_events, health, and static_metadata endpoint kinds"
  - T7: "srd032-tool-metrics-monitor-store R1.4, R2.6, R4.4 -- cached snapshots, recorder boundaries, and export diagnostics"
  - T8: "cfg-rest-tool-format rest_server -- profile-owned server routes, limits, auth, and OpenAPI binding"

success_criteria:
  - id: S1
    criterion: Monitor REST routes read cached monitor state and do not execute tools or MachineSpec actions.
    traces:
      - srd033-monitor-rest-api AC1
  - id: S2
    criterion: OpenAPI output documents every configured monitor endpoint without exposing secret or prompt data.
    traces:
      - srd033-monitor-rest-api AC2
  - id: S3
    criterion: Snapshot endpoints expose machine, tool, metric, aggregate, and recent-event views from the monitor store.
    traces:
      - srd033-monitor-rest-api AC3
  - id: S4
    criterion: Event streams emit cached monitor updates without creating workflow signals or keeping tools running.
    traces:
      - srd033-monitor-rest-api AC4
  - id: S5
    criterion: REST failures and redaction failures do not change command results, monitor store state, or MachineSpec state.
    traces:
      - srd033-monitor-rest-api AC5

out_of_scope:
  - Writing monitor state through REST.
  - Dispatching tools from monitor HTTP handlers.
  - Implementing REST handlers in this documentation issue.
  - Defining durable metric history.

semantic_model: sm-tool-metrics-monitoring
config_format: cfg-rest-tool-format
