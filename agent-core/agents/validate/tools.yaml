# Tool selection for the validate state machine.
# Used with: agent --machine agents/validate/machine.yaml --tools agents/validate/tools.yaml
# Declarations live in tools/builtin.yaml.

tools:
  - load_corpus
  - validate_specs
  - format_report
