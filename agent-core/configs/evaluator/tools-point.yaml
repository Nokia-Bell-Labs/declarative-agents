# Tool selection for the evaluator point machine (point.yaml).
# Loaded by run_point to build the nested point registry.

tools:
  - create_point_dir
  - copy_sample_workspace
  - copy_sample_docs
  - init_workspace_repo
  - stage_workspace_baseline
  - commit_workspace_baseline
  - dump_config
  - run_agent
  - check_results
  - collect_metrics
