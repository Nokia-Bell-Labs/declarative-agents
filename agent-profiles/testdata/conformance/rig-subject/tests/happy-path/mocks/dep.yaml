# The dependency answers. The fixture name decides the variable the rig
# publishes the twin's address as: dep.yaml -> DEP_URL, which the subject's
# rest.yaml declares as base_url: ${DEP_URL:-...}.
routes:
  - method: GET
    path: /v1/record
    responses:
      - status: 200
        body:
          state: present
