# Temporary oversized Go test suites. Each entry is assigned to an explicit
# split issue; the guard rejects new entries and rejects stale entries after a
# split brings a file under 500 lines. The baseline is currently empty.
