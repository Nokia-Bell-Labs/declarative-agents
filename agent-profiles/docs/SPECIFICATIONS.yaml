id: agent-profiles-specifications
title: Agent Profiles Specification Index

overview: |
  This index starts the specification corpus for profile-owned agent assets.
  Foundation documents come first because the migration needs a stable
  ownership boundary before assets move.

  With the first migration complete, the SRD layer defines migrated profile
  families as functional blocks instead of runtime packages. srd001 now holds
  the cross-cutting profile ownership boundary and dependency-direction
  invariants, while srd002 through srd011 own one profile family each. The
  semantic models record role boundaries, dependency directions, and validation
  views.

  Release 06.0 now has formal use cases and a test suite for the checked-in
  profile repository workflow. Those entries connect profile validation,
  mounted container smoke execution, profile bundle tagging, and agent-core
  integration consumption back to the SRD and semantic models.
  The traceability gap is closed.

  Release 07.0 implements profile-owned integration tracer bullets for concrete
  state machines and profile-to-profile boundaries. The implemented use cases
  cover documentation-curator UX, evaluator to generator, planner to generator,
  bench to evaluator, and monitor/control evidence that lives with the profile
  assets.

  A reader can now start at the SRD, follow the semantic model role entries,
  reach the two mounted-profile use cases, and land on named commands such as
  mage validate, mage containerSmoke, mage tag, and the agent-core integration
  targets that set AGENT_PROFILES_ROOT.

roadmap_summary:
  - version: "00.1"
    name: Repository bootstrap
    status: done
    scope: |
      Replace the template README, add the foundation docs needed for future
      structured issues, migrate profile assets, add release tagging, and
      validate mounted-profile references.
  - version: "06.0"
    name: External agent profiles and mounted runtime assets
    status: done
    scope: |
      Profile-owned agent assets, demos, and integration fixtures now live in
      this repository. agent-core consumes them from AGENT_PROFILES_ROOT,
      /profiles, or release bundles. Formal use cases and test suites now name
      executable proof for the completed migration path.
  - version: "07.0"
    name: Profile-owned integration tracer bullets
    status: done
    scope: |
      Implemented profile-owned tracer bullets for concrete profile state
      machines, profile-to-profile execution, and human/operator boundaries.
      The release proof is the profile-owned Mage integration target set.
  - version: "07.1"
    name: Per-family SRDs and profile coverage
    status: in-progress
    scope: |
      Per-family SRDs replace the shared functional-block SRD, and per-family
      boundary use cases plus a test suite cover the jurist, lifecycle, and rest
      families that release 07.0 left to the generic mounted-profile and
      profile-validation use cases. Profile-owned Mage integration targets for
      the three families remain a deferred follow-up.

foundation_document_index:
  - id: agent-profiles
    title: Agent Profiles Repository Vision
    summary: Goals, boundaries, ownership, and success criteria for external
      profile assets.
    path: docs/VISION.yaml
  - id: architecture-agent-profiles
    title: Agent Profiles Architecture
    summary: Repository layout, runtime mount interfaces, ownership decisions,
      and implementation status.
    path: docs/ARCHITECTURE.yaml
  - id: agent-profiles-roadmap
    title: Agent Profiles Release Road Map
    summary: Bootstrap and migration release plan.
    path: docs/road-map.yaml
  - id: issue-format
    title: Issue Format Constitution
    summary: Required issue fields and scope rules for future work.
    path: docs/constitutions/issue-format.yaml

srd_index:
  - id: srd001-agent-functional-blocks
    title: Agent Functional Blocks
    summary: Cross-cutting profile ownership boundary and cross-profile
      dependency-direction invariants shared by every profile family. Per-family
      requirements live in srd002 through srd011.
    status: implemented
    path: docs/specs/software-requirements/srd001-agent-functional-blocks.yaml
  - id: srd002-generator
    title: Generator Functional Block
    summary: Generator coding-agent boundary, inputs, tool selections, runtime
      dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd002-generator.yaml
  - id: srd003-evaluator
    title: Evaluator Functional Block
    summary: Evaluator session and point pipeline boundary, inputs, tool
      selections, runtime dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd003-evaluator.yaml
  - id: srd004-planner
    title: Planner Functional Block
    summary: Planner requirement-graph boundary, inputs, tool selections,
      runtime dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd004-planner.yaml
  - id: srd005-jurist
    title: Jurist Functional Block
    summary: Jurist deterministic validator boundary, inputs, tool selection,
      responsibilities, provenance-rich findings, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd005-jurist.yaml
  - id: srd006-bench
    title: Bench Functional Block
    summary: Bench human-operated launcher boundary, inputs, tool selections,
      runtime dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd006-bench.yaml
  - id: srd007-rest
    title: REST Functional Block
    summary: REST client and server boundary words, inputs, tool selections,
      profile-owned authority, outcomes, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd007-rest.yaml
  - id: srd008-monitor
    title: Monitor Functional Block
    summary: Monitor read-only REST reader boundary, inputs, tool selections,
      runtime dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd008-monitor.yaml
  - id: srd009-lifecycle
    title: Lifecycle Functional Blocks
    summary: Lifecycle history, rollback, and approval machine-action
      boundaries, inputs, tool selections, runtime dependencies, outputs, and
      terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd009-lifecycle.yaml
  - id: srd010-control
    title: Control Functional Block
    summary: Control signal-intake boundary, inputs, tool selections, runtime
      dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd010-control.yaml
  - id: srd011-knowledge-manager
    title: Knowledge Manager Functional Block
    summary: Documentation-curator UX and REST workflow boundary, inputs, tool
      selections, runtime dependencies, outputs, and terminal outcomes.
    status: implemented
    path: docs/specs/software-requirements/srd011-knowledge-manager.yaml

config_format_index: []

semantic_model_index:
  - id: sm-multi-agent-profile-system
    title: Multi-Agent Profile System
    summary: Role groups, actor boundaries, dependency directions, and
      validation views for profile-owned agent programs.
    status: implemented
    path: docs/specs/semantic-models/multi-agent-profile-system.yaml
  - id: sm-agent-role-taxonomy
    title: Agent Role Taxonomy
    summary: Per-profile role, inputs, outputs, selected tool families,
      runtime dependencies, outcomes, and checks.
    status: implemented
    path: docs/specs/semantic-models/agent-role-taxonomy.yaml

use_case_index:
  - id: rel06.0-uc001-mounted-profile-execution
    title: Mounted Profile Execution
    summary: Mounted execution path for running agent-core with profiles,
      demos, fixtures, and workspaces supplied from agent-profiles.
    status: implemented
    path: docs/specs/use-cases/rel06.0-uc001-mounted-profile-execution.yaml
  - id: rel06.0-uc002-profile-validation
    title: Profile Validation and Release Bundle Readiness
    summary: Validation path for profile reference checks, mounted container
      smoke execution, and profile bundle tag readiness.
    status: implemented
    path: docs/specs/use-cases/rel06.0-uc002-profile-validation.yaml
  - id: rel07.0-uc001-documentation-curator-profile-ux
    title: Documentation Curator Profile UX Tracer
    summary: Profile-owned Knowledge Manager browser, machine_request, trace,
      and lifecycle proof.
    status: implemented
    path: docs/specs/use-cases/rel07.0-uc001-documentation-curator-profile-ux.yaml
  - id: rel07.0-uc002-evaluator-generator-profile-boundary
    title: Evaluator to Generator Profile Boundary
    summary: Evaluator runs generator as the benchmark subject profile.
    status: implemented
    path: docs/specs/use-cases/rel07.0-uc002-evaluator-generator-profile-boundary.yaml
  - id: rel07.0-uc003-planner-generator-profile-boundary
    title: Planner to Generator Profile Boundary
    summary: Planner materializes a task and executes it through generator.
    status: implemented
    path: docs/specs/use-cases/rel07.0-uc003-planner-generator-profile-boundary.yaml
  - id: rel07.0-uc004-bench-evaluator-profile-boundary
    title: Bench to Evaluator Profile Boundary
    summary: Bench launches evaluator from a human/operator boundary action.
    status: implemented
    path: docs/specs/use-cases/rel07.0-uc004-bench-evaluator-profile-boundary.yaml
  - id: rel07.0-uc005-monitor-control-profile-boundary
    title: Monitor Control Profile Boundary
    summary: Monitor/control profile boundary and lifecycle tracer evidence.
    status: implemented
    path: docs/specs/use-cases/rel07.0-uc005-monitor-control-profile-boundary.yaml
  - id: rel07.1-uc001-jurist-profile-boundary
    title: Jurist Profile Boundary
    summary: Deterministic, LLM-free charter validation from load through report
      to a Passed or Failed terminal state.
    status: specified
    path: docs/specs/use-cases/rel07.1-uc001-jurist-profile-boundary.yaml
  - id: rel07.1-uc002-lifecycle-profile-boundary
    title: Lifecycle Profile Boundary
    summary: History, rollback, and approval as machine actions with explicit
      Approved or Rejected resume signals.
    status: specified
    path: docs/specs/use-cases/rel07.1-uc002-lifecycle-profile-boundary.yaml
  - id: rel07.1-uc003-rest-profile-boundary
    title: REST Profile Boundary
    summary: Configured client and server boundary words, profile-owned REST
      authority, OpenAPI imports, and the ollama-* variant.
    status: specified
    path: docs/specs/use-cases/rel07.1-uc003-rest-profile-boundary.yaml

test_suite_index:
  - id: test-rel06.0-agent-profiles
    title: Test Suite for Release 06.0 Agent Profiles
    summary: Executable proof index for mage validate, mage containerSmoke,
      mage tag, and agent-core integration targets that consume
      AGENT_PROFILES_ROOT.
    status: implemented
    path: docs/specs/test-suites/test-rel06.0-agent-profiles.yaml
  - id: test-rel07.0-profile-integrations
    title: Test Suite for Release 07.0 Profile Integrations
    summary: Implemented profile-owned tracer bullets for
      documentation-curator UX, evaluator-generator, planner-generator,
      bench-evaluator, and monitor/control boundaries.
    status: implemented
    path: docs/specs/test-suites/test-rel07.0-profile-integrations.yaml
  - id: test-rel07.1-profile-boundaries
    title: Test Suite for Release 07.1 Profile Boundaries
    summary: Boundary use-case validation for the jurist, lifecycle, and rest
      families against their per-family SRD acceptance criteria.
    status: specified
    path: docs/specs/test-suites/test-rel07.1-profile-boundaries.yaml
  - id: test-rel07.1-profile-conformance
    title: Test Suite for Release 07.1 Profile Conformance
    summary: Executable per-family OpenTelemetry conformance for all ten profile
      families, traced to srd002-srd011 and sm-agent-role-taxonomy; integration
      target mage conformance. Nine families implemented (generator
      Ollama-gated, lifecycle suspend/resume Dolt-gated); rest specified pending
      an agent-core gap.
    status: implemented
    path: docs/specs/test-suites/test-rel07.1-profile-conformance.yaml

coverage_gaps: |
  The SRD and semantic models describe ownership, role boundaries, and
  functional-block requirements for migrated profile families. Profile assets,
  demos, integration fixtures, release tagging, profile validation, and
  mounted-container smoke checks are implemented.

  Release 06.0 traceability now covers mounted profile execution and profile
  validation. It also covers mounted container smoke, profile bundle tagging,
  and agent-core integration targets that consume this repository through
  AGENT_PROFILES_ROOT.
  No missing proof remains for this release.

  Future gaps should track new profile families, demos, fixtures, or validation
  commands as they are added. Existing release coverage no longer depends on a
  deferred follow-up issue.

  Release 07.0 implements the documentation-curator profile UX tracer,
  evaluator-to-generator boundary tracer, planner-to-generator boundary tracer,
  bench-to-evaluator launch tracer, and monitor/control boundary tracer. The
  monitor/control target records profile-owned configuration evidence while
  agent-core retains runtime ownership for REST and lifecycle builtin behavior.

  Release 07.1 adds per-family boundary use cases for the jurist, lifecycle, and
  rest families, which release 07.0 covered only through the generic rel06.0
  mounted-profile and profile-validation use cases. Every profile family now has
  a dedicated boundary use case: evaluator, generator, planner, bench,
  monitor/control, and documentation-curator through release 07.0, and jurist,
  lifecycle, and rest through release 07.1. test-rel07.1-profile-boundaries
  validates the three new use cases against their per-family SRD acceptance
  criteria. The remaining gap is profile-owned Mage integration targets that
  execute the jurist, lifecycle, and rest boundary fixtures; until those land the
  three use cases and their test cases stay status specified rather than
  implemented.

  Release 07.1 also adds test-rel07.1-profile-conformance, an executable
  per-family conformance suite run by mage conformance. It drives each of the ten
  families through the agent-core CLI and asserts on the captured OpenTelemetry
  trace (span vocabulary and terminal state), tracing every family to its
  srd002-srd011 acceptance criteria and sm-agent-role-taxonomy role. Nine
  families are implemented hermetically under AGENT_CORE_ROOT (generator is
  Ollama-gated, lifecycle suspend/resume and history/rollback are Dolt-gated).
  Planner conformance landed once agent-core gained a load_graph pipeline word
  that seeds the requirement graph, and lifecycle history/rollback landed once
  agent-core routed the --request checkpoint id and to_iteration into a
  target-pinned checkpoint backend and the tool configs. The rest family stays
  status specified, blocked on the agent-core async await_operation gap.
