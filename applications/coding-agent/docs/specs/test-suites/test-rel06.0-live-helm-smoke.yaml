# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: test-rel06.0-live-helm-smoke
title: Packaged-Chart Live Helm Smoke Suite
release: "06.0"
status: implemented

overview: |
  Deterministic unit tests pin prerequisite skips, infrastructure probes,
  semantic classification, bounded diagnostics, canonical profile-free image
  construction, and kind fixtures. The live target supplies the Kubernetes
  evidence: one canonical agent image ID across every agent and mock pod, the
  executor's tools mounted from donors, and the model boundary served by the
  canonical mock profile. The one-image construction and mock-profile evidence
  are delivered by the smoke refactor (GH-2496) and the model-mock deployment
  (GH-2501); this suite states the contract those units satisfy.

traces:
  - rel06.0-uc001-live-helm-smoke

test_cases:
  - use_case: rel06.0-uc001-live-helm-smoke
    name: Missing prerequisite skip
    go_test: go test ./magefiles -run '^TestCodingHelmSmokeSkipsMissingExternalPrerequisite$'
    status: implemented
    traces: [srd005-live-helm-smoke AC2]
  - use_case: rel06.0-uc001-live-helm-smoke
    name: Failure classification and diagnostics
    go_test: go test ./magefiles -run '^TestCodingHelm(InfrastructureProbeClassification|FailureDistinguishesSemanticFromInfrastructure|DiagnosticsUseOneBoundedContext)$'
    status: implemented
    traces: [srd005-live-helm-smoke AC3]
  - use_case: rel06.0-uc001-live-helm-smoke
    name: Canonical image and kind fixtures
    go_test: go test ./magefiles -run '^TestCodingHelm(FixturesMatchServingAndStorageContract|SmokeUsesProductionProfileFreeImageRecipe)$'
    status: implemented
    traces: [srd005-live-helm-smoke R2]
  - use_case: rel06.0-uc001-live-helm-smoke
    name: Full packaged topology
    integration_target: mage integration:helmSmoke
    status: implemented
    traces: [srd005-live-helm-smoke AC1]

preconditions:
  - Docker, kind, Helm, kubectl, and the required local images are available.
  - The agent-core and applications/catalog checkouts are present.
