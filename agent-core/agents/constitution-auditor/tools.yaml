# Tool selection for the validate state machine.
# Used with: agent --machine agents/constitution-auditor/machine.yaml --tools agents/constitution-auditor/tools.yaml
# Declarations live in tools/builtin.yaml.

tools:
  - load_corpus
  - validate_specs
  - format_report
