# Tool selection for the evaluator session machine (machine.yaml).
# Per-point tools live in configs/evaluator/tools-point.yaml and are loaded
# by run_point for the nested point machine.

tools:
  - load_suite
  - next_point
  - run_point
  - report_session
