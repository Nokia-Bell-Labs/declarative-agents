# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: srd004-helm-package-validation
title: Helm Values and Package Validation
status: implemented

problem: |
  A rendered source chart is insufficient release evidence. Values must reject
  unsafe or incoherent topology, prepared profile packages must resist stale or
  tampered metadata and content, and the installable archive must include every
  role asset and render independently of the repository.

requirements:
  R1:
    title: Strict values
    items:
      - R1.1: values.schema.json shall reject unknown keys and malformed image, replica, port, resource, storage, URL, mount, workspace, model, security, and telemetry values.
      - R1.2: Schema conditions shall reject external model mode without an HTTP endpoint.
      - R1.3: Template guards shall retain mount, fixed-role-port, telemetry, and model invariants when schema validation is bypassed.

  R2:
    title: Package integrity
    items:
      - R2.1: Role manifests shall carry a deterministic checksum over ordered path and content bytes.
      - R2.2: Preparation shall strictly parse manifests and reject stale roles, unknown fields, missing or extra files, checksum mismatch, duplicate coverage, invalid partition sizes, and encoded-key conflicts.
      - R2.3: Prepared package validation shall run before chart staging or packaging.

  R3:
    title: Self-contained Helm archive
    items:
      - R3.1: mage helm:package shall regenerate role packages, stage the chart, run default, CI, and valid-fixture renders, and create one chart archive.
      - R3.2: Archive validation shall require Chart.yaml, values, schema, core templates, and every generated profile package file and shall reject links.
      - R3.3: The resulting .tgz shall lint and render from a directory with no source checkout.

acceptance_criteria:
  - id: AC1
    criterion: Every valid fixture lints and renders, and every invalid fixture fails with its intended field.
    traces: [R1.1, R1.2, R1.3]
  - id: AC2
    criterion: Deterministic tamper tests reject stale manifests, files, partitions, checksums, conflicts, and omissions.
    traces: [R2.1, R2.2, R2.3]
  - id: AC3
    criterion: A packaged .tgz contains all role assets and independently passes helm lint and helm template.
    traces: [R3.1, R3.2, R3.3]

goals:
  - id: G1
    text: Strict values.
  - id: G2
    text: Package integrity.
  - id: G3
    text: Self-contained Helm archive.
non_goals:
  - Live Kubernetes installation.
  - kind cluster setup and teardown.
  - Operator deployment and teardown guide.
