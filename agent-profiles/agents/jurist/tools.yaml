# Tool selection for the validate state machine.
# Used with: agent --machine agents/jurist/machine.yaml --tools agents/jurist/tools.yaml
# Declarations live in tools/builtin.yaml.

tools:
  - load_corpus
  - validate_specs
  - jurist_rg
  - reduce_grep_checks
  - format_report
